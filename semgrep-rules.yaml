rules:
  - id: typescript.security.generic
    languages: [typescript, javascript]
    message: Generic security check
    severity: WARNING
    patterns:
      - pattern-either:
          - pattern: eval($X)
          - pattern: document.write($X)
          - pattern: innerHTML = $X
          - pattern: exec($X)

